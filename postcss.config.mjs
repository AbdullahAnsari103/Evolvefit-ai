// In TailwindCSS v4, the PostCSS plugin is included in the 'tailwindcss' package
/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
}

export default config
